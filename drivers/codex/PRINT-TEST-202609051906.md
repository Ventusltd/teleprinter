# Tested app-only printing and original tool layers: 202609051906

The deployed candidate adds GRID/SUBS quick controls, initially collapsed layers and separate original GIS SLD Financial Sandbox, Module Layout and Cable Geometry app layers. The original tool files remain separately pinned and are not merged into Atlas calculations. Straight-line engine behaviour is unchanged.

50 actual Chrome visits passed (25 PDF,25 source downloads), with display sharing and host screenshot injection forbidden. All25 source prints passed independent offline audit. Desktop and phone viewport control checks passed for all three original tools. A separate manual Chrome visit exercised Cable Geometry inputs and downloaded an Atlas app-only PDF after closing the tool: embedded image3840x1906, generation1906.

Full campaign: offline-screenshots/architecture-reload-20260905/five-version-campaign/1906-full-settled/teleprinter-2026-09-05T19-20-50-856Z-5972/campaign-results.json. Compact measured results are in fifty-prints-summary-202609051906.json. Raw PDFs, screenshots and source downloads stay offline.

The previous50-visit attempt had one Pipeline portrait comparison failure during chart/scroll settling. It is retained under1906-full. The test harness now waits for a stable chart and scroll state; the85percent regional pixel tolerance was not reduced. Original app readiness failures and earlier harness failures also remain in the evidence folder.

## Limits

DOM rendering is an approximate visual comparison, not compositor pixel equality. Tests use desktop browsers and phone viewports, not physical phones. Printing inside original iframe tools is outside this campaign; their original print behaviour is retained. Their complete source lives in producer releases; Atlas source export is not an exhaustive recursive export of opened iframe dependencies.

Testcode fleet CI is separately red: missing checkout inputs and an existing Atlas cartridge character-budget failure. These candidate tests do not override it. No Design Freeze is claimed. Optional constrained routes and trench-versus-drill analysis remain planned, alongside the preserved straight-line first pass.

## Ownership

GIS: Ventusltd/gis-sld-sandbox release202609051855. Module Layout and this candidate's Cable Geometry: Ventusltd/layout-tool release202609051858. Dedicated Cable ownership is being migrated in a later immutable candidate. Integration: Testcode sandbox/capsules/tool-layers. Print/control cartridges: Teleprinter drivers/codex. Architecture and CI ledger: Spiders codex/reload.
