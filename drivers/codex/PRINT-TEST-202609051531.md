# Print candidate 202609051531: measured results, not Design Freeze

Published candidate: https://globalgrid2050.com/testcode/202609051531/
Both commands stay in File: Print PDF and Print source code. The engine name
is internal; there is no floating Teleprinter launcher in this candidate.

50 installed-Chrome visits completed: 25 actual PDF downloads and 25 actual
source downloads, one fresh browser per visit, each closed. The PDF tests use
an in-memory host capture of the actual rendered viewport. Every embedded
pixel and rendered PDF pixel matched, and the header/footer remained outside
the unchanged image. Tests require the requested completed REPD project,
capture-time layer selection, matching map visibility and an onscreen legend.
Source tests compare the captured state, full frame hashes and pinned source.

An independent offline batch rechecked all 25 original source downloads against
their receipts and local Git, then parsed JavaScript without executing it and
parsed JSON: 25/25 passed in 44.05 seconds. Separate actual File-menu checks in
Edge, Firefox and WebKit passed 6/6 using the host capture route. These are not
physical iPhone/Android tests or native-capture fidelity passes.

Native screen capture remains blocked. Exact live module-response bytes were
verified in three Chrome visits; Pipeline 1200x800 passed, both Atlas cases
failed. The desktop case refused a 1364x898 frame when 1365x900 was required.
The mobile case exposed an undefined-rejection error; the engine repository
now handles that safely, proven failing then passing by screen-capture.test.mjs.
That later correction has not been promoted into immutable 1531. No reduced
frame was quietly upscaled, and no host-capture pass clears this requirement.

The full Design Freeze gate reread every artifact and rejected 1531 solely for
CODEX-NATIVE-001. Therefore this is a comparison candidate, not a frozen release.

Identities:

- App source: `6c2cc1453129378e8e8bc6413af401e7d2c7e333` in testcode.
- Candidate engine: `97ef8043a3f29ec162932838b44b2d6163845727` in teleprinter.
- Build inventory SHA256: `da8e97ba58b3e06e7851f783c72b83e78488e5f533ea70e7c0ba95c20c72e122`.
- Published bytes: 152/152 matched globalgrid2050 commit `9346073112e0561122ff2ff992df69a9f9969f78`.

The full campaign receipt is fifty-prints-summary-202609051531.json (the full receipt remains offline). Browser downloads,
PNG references and source TXT files remain under the Desktop offline-screenshots
folder, in teleprinter-2026-09-05T15-37-01-522Z-10396. They are not in Git.

The user's source TXT independently passed 52 full-body checks, 24 JavaScript
parses, 13 JSON parses and local Git comparison. The user's older iPhone PDF
was readable on 591/591 pages but contained 21 truncated resources, including
four scripts, and three recorded dependency 404s. Those are separate generations
and independent drivers; their findings are never substituted for each other.

See OFFLINE-RUNNERS.md for repeatable no-network, no-AI checks. Browser source
discovery still cannot prove all worker, computed, dormant or server dependencies.
