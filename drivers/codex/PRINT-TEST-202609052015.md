# Tested independent tool cartridges: 202609052015

Live candidate 202609052015 passed 50 actual Chrome visits: 25 app-only PDF downloads and 25 source downloads. All 25 source prints also passed the independent offline source audit. Screen sharing and injected host screenshots were forbidden in the campaign. Approximate PDF visual comparisons retain the existing thresholds; these are not compositor pixel-equivalence claims.

The candidate adds independently versioned Module draw readiness, Cable render completion, the original GIS read-only manual-route adapter, and a layer navigation resolver. Desktop and phone viewport checks passed direct-line drawing, two manual route pins, commit/clear, immutable route snapshots, Cable snapshot hashes, original sibling navigation, current-document restart and Atlas layer-state retention. The straight-line first pass remains unchanged.

The 208 deployed files matched the committed candidate. The first deployment attempt for predecessor2011 built successfully but its deploy step timed out acquiring GitHub's OIDC token. The succeeding2015 deployment served both retained releases; both later passed their complete byte and live-control checks. That infrastructure failure remains recorded separately from product test results.

Raw campaign files: offline-screenshots/architecture-reload-20260905/next-fifty/campaign-2015/teleprinter-2026-09-05T20-22-01-183Z-44976. Independent audit: next-fifty/source-batch-2015/summary.json. Compact campaign results: fifty-prints-summary-202609052015.json. Raw PDFs, screenshots and diagnostic downloads remain offline.

## Limits

Original iframe tool printing is outside this full campaign. Their original browser-print paths remain until separately implemented and tested. Phone tests use Chrome viewports, not physical devices. Runtime source diagnostic downloads remain large; the later standalone source-index/reader releases provide separate per-file inspection but do not yet impose the planned diagnostic export budget. The constrained-routing and trench/drill models are future work, not construction acceptance. Existing unrelated fleet CI failures are not waived by these candidate passes; no Design Freeze is claimed.

## Source locations

GIS: gis-sld-sandbox/src/gis-sld and immutable releases. Module: layout-tool/src/module-layout. Cable: cable-trench-or-drill/src/cable-geometry. Layer integration/build cartridges: testcode sandbox/capsules/tool-layers and sandbox/capsules/teleprinter. Print/control and Chrome harness: teleprinter/drivers/codex. Version plan and CI receipts: spiders/codex/reload/plan-tracker.
